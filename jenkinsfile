pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking In...'
                checkout([$class: 'GitSCM', branches: [[name: '*/jenkins']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/ksharma20/MoviesAdda.git']]])
            }
        }
        stage('Build') {
            steps {
                echo 'Building....'
                git branch: 'jenkins', url: 'https://github.com/ksharma20/MoviesAdda.git'
                sh setup.sh
                sh 'python manage.py runserver'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing....'
                sh 'python manage.py test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}